/**
 * @fileoverview Centralized mock data fixtures
 * @internal - For development and testing only
 *
 * All fixtures extracted during component refactoring (Prompt 1)
 * Use these instead of inline mock data in components
 */

export * from './launch.fixtures';

// TODO: Add fixtures for other oversized components:
// - tools.fixtures.ts (CodeGeneratorTool, FullStackAppBuilder, AgentDesignerTool)
// - user.fixtures.ts (UserPersonaCards)
// - print-on-demand.fixtures.ts (PrintDesignSuite)
// - marketing.fixtures.ts (LaunchMarketingCampaign)
// - deployment.fixtures.ts (OneClickDeployTool)
// - monitoring.fixtures.ts (AdvancedMonitoringSystem)
// - collaboration.fixtures.ts (AdvancedCollaborationHub)
